import LoginForm from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Sign In - FreePromptBase',
  description: 'Sign in to your FreePromptBase account to share and discover AI prompts.',
};

export default function LoginPage() {
  return <LoginForm />;
}

