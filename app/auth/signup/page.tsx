import SignupForm from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Sign Up - FreePromptBase',
  description: 'Create a free FreePromptBase account to join the AI prompt community.',
};

export default function SignupPage() {
  return <SignupForm />;
}

