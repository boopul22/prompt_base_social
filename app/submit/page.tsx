import SubmitPrompt from '@/components/SubmitPrompt';

export const metadata = {
    title: 'Submit a Prompt - FreePromptBase',
};

export default function SubmitPage() {
    return <SubmitPrompt />;
}
